#include "LinkedSortedArrays.h"
#include <iostream>
int main() {
	int K;
	cin >> K;
	LinkedSortedArrays<char*> items = LinkedSortedArrays<char*>(K);

	char* userInput = new char[256];
	cin >> userInput;
	while (userInput[0] != '\0') {

	}
}